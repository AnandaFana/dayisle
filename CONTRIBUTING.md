# 参与时屿

欢迎反馈使用体验、提交问题或 Pull Request。界面以中文为主，代码保持小模块、少依赖，运行依赖限定为 Python 标准库。

## 本地开发

使用独立目录启动：`python run.py --no-browser --data-dir ../dayisle-dev-data`。体验示例只读，修改流程请使用虚构记录测试。

提交涉及数据、日期、层级关系、导入或交互状态的变更时，运行 README 中的 Python 与 JavaScript 测试，并添加能验证用户行为的必要用例。界面变更请检查薄荷、纸艺、素白、赛博朋克主题，以及窄屏布局。

## 提交内容

- 保持用户已有入口与数据格式兼容；涉及存储迁移时说明恢复方法。
- 用 CSS 变量适配主题，用 config.toml 管理默认配置。
- 问题与 PR 说明复现步骤、修改后的行为及验证方式。
- 截图只使用虚构示例；不要提交个人日记、记录、备份、日志、草稿或密钥。
- 自定义数据目录请放在仓库外。提交前检查 `git diff --cached`，不要强制添加被忽略的数据文件。

贡献内容采用仓库的 MIT 许可证。
